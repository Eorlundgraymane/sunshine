- args:
    permission:
      columns:
      - id
      - friend_id
      - created
      filter:
        id:
          $eq: X-HASURA-USER-ID
    role: user
    table: friends
  type: create_update_permission
