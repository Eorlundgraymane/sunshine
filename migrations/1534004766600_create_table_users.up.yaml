- args:
    sql: CREATE TABLE "users"("hasura_id" integer NOT NULL, "mobile" text NOT NULL,
      "created" text NOT NULL DEFAULT 'now()', PRIMARY KEY ("hasura_id") )
  type: run_sql
- args:
    name: users
  type: add_existing_table_or_view
