- args:
    sql: CREATE TABLE "friends"("id" integer NOT NULL, "friend_id" integer NOT NULL,
      "created" text NOT NULL DEFAULT 'now()', PRIMARY KEY ("id","friend_id") )
  type: run_sql
- args:
    name: friends
  type: add_existing_table_or_view
